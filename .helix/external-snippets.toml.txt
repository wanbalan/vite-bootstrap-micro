[[sources]] # list of sources to load
name = "friendly-snippets"  # optional name shown on snippet description
git = "https://github.com/rafamadriz/friendly-snippets.git" # git repo with snippets collections

[[sources.paths]] # list of paths to load on current source
scope = ["bash"]  # optional scopes for current snippets
path = "snippets/bash/bash.json"  # where snippet file or dir located in repo

[[sources.paths]] # list of paths to load on current source
scope = ["python"]  # optional scopes for current snippets
path = "snippets/python/python.json"  # where snippet file or dir located in repo

[[sources.paths]] # list of paths to load on current source
scope = ["javascript"]  # optional scopes for current snippets
path = "snippets/javascript/javascript.json"  # where snippet file or dir located in repo

[[sources.paths]] # list of paths to load on current source
scope = ["html"]  # optional scopes for current snippets
path = "snippets/html.json"  # where snippet file or dir located in repo

